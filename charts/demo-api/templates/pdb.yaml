apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: {name: demo-api, namespace: {{ .Release.Namespace }}}
spec:
  minAvailable: 1
  selector:
    matchLabels: {app.kubernetes.io/name: demo-api}
